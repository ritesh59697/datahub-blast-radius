# Migration plan: `orders.order_date` → `order_dt`

Generated by [Blast Radius](https://github.com/ritesh59697/datahub-blast-radius) from DataHub lineage. **21 downstream entities** across **7 platforms** reference this column.

> Ordered nearest-first: each step assumes the ones above it have landed. The SQL below is a starting point for review — DataHub knows *that* a column is consumed, not the shape of every query consuming it. Read each change before applying it.

## Backwards-compatible rollout (recommended)

Renaming in place breaks every consumer at once. Shipping the alias first lets consumers migrate independently:

```sql
-- Step 1: expose both names from orders
select
    order_date as order_dt,
    order_date,  -- deprecated: remove after consumers migrate
    ...
from {{ ref('orders') }}
```

Then work through the consumers below, and drop `order_date` in a follow-up PR once none remain.

## Consumers to update

### s3

1. **orders** (Dataset, 1 hop)
   - update any `select` referencing `order_date`

### spark

2. **export_table_orders_to_s3** (Data Job, 1 hop)
   - check the job's column mapping and re-run a backfill if needed
3. **import_table_orders_to_snowflake** (Data Job, 2 hops)
   - check the job's column mapping and re-run a backfill if needed

### snowflake

4. **ORDERS** (Dataset, 2 hops)
   - update any `select` referencing `order_date`
5. **ORDER_DETAILS** (Dataset, 3 hops) — owner: DataHub SE Team, David Kim
   - update any `select` referencing `order_date`
6. **ORDER_DETAILS_REPLICA** (Dataset, 4 hops)
   - update any `select` referencing `order_date`

### dbt

7. **orders** (Dataset, 3 hops) — owner: Data Platform Team, Ian Chen
   - update any `select` referencing `order_date`
8. **order_details** (Dataset, 4 hops) — owner: DataHub SE Team, Data Platform Team
   - update any `select` referencing `order_date`

### looker

9. **order_details** (Dataset, 4 hops) — owner: Fiona Green, Ian Chen
   - update any `select` referencing `order_date`
10. **Order Details** (Dataset, 5 hops) — owner: Data Platform Team, Backend Engineering Team
   - update any `select` referencing `order_date`
11. **Orders by Day** (Chart, 6 hops)
   - repoint the field binding to `order_dt`
12. **Order Entry Dashboard** (Dashboard, 6 hops)
   - verify tiles render after the upstream charts are fixed

### powerbi

13. **ORDER_DETAILS** (Dataset, 4 hops) — owner: Karen Okonkwo
   - update any `select` referencing `order_date`
14. **Customer Analytics Measures** (Dataset, 4 hops)
   - update any `select` referencing `order_date`
15. **Essential KPI Measures** (Dataset, 4 hops)
   - update any `select` referencing `order_date`
16. **Geographic Measures** (Dataset, 4 hops)
   - update any `select` referencing `order_date`
17. **Product Perfromance Measures** (Dataset, 4 hops)
   - update any `select` referencing `order_date`
18. **Time Inteligence Measures** (Dataset, 4 hops)
   - update any `select` referencing `order_date`

### tableau

19. **Custom SQL Query** (Dataset, 4 hops)
   - update any `select` referencing `order_date`
20. **Orders By Day** (Dataset, 5 hops) — owner: David Kim
   - update any `select` referencing `order_date`
21. **Orders By Month** (Chart, 6 hops) — owner: David Kim
   - repoint the field binding to `order_dt`

## Notify

- **Ian Chen** (`ian.chen@example.com`) — 4 affected asset(s)
- **David Kim** (`david.kim@example.com`) — 4 affected asset(s)
- **Data Platform Team** (`data-platform@example.com`) — 4 affected asset(s)
- **Karen Okonkwo** (`karen.okonkwo@example.com`) — 2 affected asset(s)
- **Julia Novak** (`julia.novak@example.com`) — 2 affected asset(s)
- **Fiona Green** (`fiona.green@example.com`) — 2 affected asset(s)
- **DataHub SE Team** — 2 affected asset(s)
- **Sarah Chen** (`sarah.chen@example.com`) — 1 affected asset(s)

## Verification

```bash
# Re-run the analysis; it should report no remaining consumers
blast-radius analyze --column orders.order_date
```
